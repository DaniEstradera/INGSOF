using UnityEngine;
using System.Collections;

public class OffsetAnim : MonoBehaviour {
	//TrailRenderer trail;
	// Use this for initialization
	void Start () {
		//trail = this.GetComponents<TrailRenderer>;
	}
	
	// Update is called once per frame
	void Update () {
		//trail.material.SetTextureOffset+=
	}
}
